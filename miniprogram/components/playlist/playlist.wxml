<!--components/playlist/playlist.wxml-->
<view class="playlist-container" bindtap="goToMusicList">
    <image src="{{pl.picUrl}}" class="playlist-img"></image>
    <text class="playlist-count">{{_count}}</text>
    <view class="playlist-name">{{pl.name}}</view>
</view>
